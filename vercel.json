{
  "buildCommand": "cp -r backend/api backend/src backend/package.json backend/package-lock.json . && npm install && cd frontend && npm install --legacy-peer-deps && npm run build:web",
  "outputDirectory": "frontend/web-build",
  "installCommand": "echo 'Dependencies installed via build command'"
}
