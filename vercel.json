{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm run build:web",
  "outputDirectory": "frontend/web-build",
  "functions": {
    "backend/api/graphql/index.ts": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/api/graphql",
      "destination": "/backend/api/graphql"
    }
  ]
}
